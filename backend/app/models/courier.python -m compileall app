from sqlalchemy.orm import Mapped, mapped_column

from app.db.session import Base


class Courier(Base):
    __tablename__ = "couriers"

    id: Mapped[int] = mapped_column(primary_key=True, index=True)щ

